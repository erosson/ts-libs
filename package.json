{
  "name": "@erosson/ts-libs",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "author": "Evan Rosson <git@erosson.org>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "release-docs": "rm -rf dist/docs && mkdir -p dist/docs/@erosson && cp docs.html dist/docs && yarn workspaces run release-docs",
    "release": "yarn workspaces run release"
  }
}